name: test
channels:
  - conda-forge
dependencies:
  # required dependencies
  - python=3.10
  - geopandas
  - pyproj
  - packaging
  # test dependencies
  - pytest
  - pytest-cov
  - pip
  - pip:
      - dask[dataframe]>=2025.1.0
