name: test
channels:
  - conda-forge
dependencies:
  # required dependencies
  - python=3.8
  - numpy=1.20
  - dask=2021.06.0
  - distributed=2021.06.0
  - geopandas=0.10
  - pandas=1.5.3
  - pygeos
  - pyproj=2.6
  - packaging
  # test dependencies
  - pytest
  - pytest-cov
  - hilbertcurve
  - pygeohash
  # optional dependencies
  - pyarrow
  - pip
  - pip:
      - pymorton
