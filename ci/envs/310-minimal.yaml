name: test
channels:
  - conda-forge
dependencies:
  # required dependencies
  - python=3.10
  - numpy=1.24
  - dask=2025.1.0
  - distributed=2025.1.0
  - geopandas=1.0.0
  - pandas=2.0.0
  - shapely=2.1
  - pyproj=3.4
  - packaging
  # test dependencies
  - pytest
  - pytest-cov
  - hilbertcurve
  - pygeohash
  # optional dependencies
  - pyarrow
  - pip
  - pip:
      - pymorton
