name: test
channels:
  - conda-forge
dependencies:
  # required dependencies
  - python=3.9
  - dask
  - distributed
  - geopandas
  - pygeos
  - pyproj=2.6
  # test dependencies
  - pytest
  - pytest-cov
  # optional dependencies
  - pyarrow
