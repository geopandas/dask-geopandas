name: test
channels:
  - conda-forge
dependencies:
  # required dependencies
  - python=3.8
  - dask
  - distributed
  - geopandas
  - pygeos
  # test dependencies
  - pytest
  - pytest-cov
  # optional dependencies
  - pyarrow
