name: test
channels:
  - conda-forge
dependencies:
  # required dependencies
  - python=3.7
  - dask=2.18
  - distributed
  - geopandas
  - pygeos
  - pyproj=2.6
  # test dependencies
  - pytest
  - pytest-cov
  - hilbertcurve
  # optional dependencies
  - pygeohash
  - pip
  - pip:
    - pymorton
